# Hush Salon — Full Project Audit
*Generated 2026-04-19*

## TL;DR

**Overall: healthy.** Backend, RLS, and edge functions all working correctly. No critical (red) findings. 6 warnings, all low-impact and most are false positives or expected configuration. Two real items worth acting on: storage bucket listing policy and the `pg_net` public-schema warning. Two code-hygiene items: `ChatTab.tsx` (1,184 lines) and `journeyTracker.ts` (201 lines) should be split.

---

## 1. Security scan (6 warnings, 0 errors)

| # | Finding | Verdict | Action |
|---|---|---|---|
| 1 | **Extension in public** — `pg_net` lives in `public` schema | Expected (Supabase default for `pg_net`/`pg_cron`). Low risk. | **No action.** Moving these requires superuser; Supabase keeps them here intentionally. |
| 2 | **Public bucket allows listing** — `site-assets` is public + listable | Real, low impact. Anyone can enumerate file names. | **Recommended:** add a `storage.objects` SELECT policy that allows reads on the bucket but blocks `LIST` (or make the bucket non-public and serve via signed URLs if file names are sensitive). |
| 3 | **callback_requests** — only `service_role` policy | False positive. Anon writes go through the `request-callback` edge function with `SUPABASE_SERVICE_ROLE_KEY`. Anon cannot read or write directly. | **No action.** Add a code comment to clarify intent. |
| 4 | **leads** — only `service_role` policy | Same as above — `submit-lead` / `capture-lead` edge functions own writes. | **No action.** |
| 5 | **artists.direct_phone** — table not publicly readable | Front-end reads via the `artists_public` view (which excludes nothing currently — see §2). | **Action:** confirm the view either excludes `direct_phone` or that exposure is intentional (specialist numbers ARE public per memory `mem://brand/contact-information`). |
| 6 | **sessions** — no anon INSERT policy | Table appears unused by client code (writes via service role only). | **No action.** Consider deprecating the table if unused. |

### Linter (Supabase): 2 warns
Both are duplicates of items above (extension in public + public bucket). No new info.

---

## 2. Database health

### Activity (last 7 days vs. total)

| table | total | last 7d | notes |
|---|---:|---:|---|
| `conversations` | 78 | 43 | Healthy chat volume. |
| `messages` | 466 | 328 | ~7.6 msgs/conversation avg this week. |
| `guest_profiles` | 24 | 10 | Returning-guest fingerprinting working. |
| `leads` | 27 | 10 | |
| `callback_requests` | 15 | 4 | |

### Schema observations
- **`artists_public` view** exists (good — separates public-readable surface from raw table). Confirm that `direct_phone` column should be in the view: per memory it's intentionally public for specialists (Kelli, Tammi, etc.), so this is **fine**. No action required, but consider documenting the policy in the view's SQL comment.
- **No DB functions, no triggers** — surprisingly clean. The `last_summarized_at`/`last_summarized_message_count` updates happen in edge functions; consider a trigger to enforce monotonic increments if you ever see backsliding.
- **`sessions` table appears orphaned.** No edge function references it; only the `STORAGE_KEY = "hush_session_id"` is in `conciergeStore.ts` but lives client-side. Candidate for deletion.

### Cron jobs
Couldn't read `cron.job` (permission denied for `psql` role) — but the recently-added `hush-daily-digest` cron entry should be there. Verify next time `#hush-leads` posts at 8 AM AZ time.

---

## 3. Edge functions (9 deployed)

`_shared`, `capture-lead`, `daily-digest`, `lead-qualify`, `luna-chat`, `request-callback`, `session-start`, `session-summarize`, `submit-lead`.

- Recent `luna-chat` logs show only routine `Shutdown` events — **no errors in the latest window.**
- The 24h analytics query returned empty (function calls happen but log-table write may be lagging or filtered) — not concerning.
- All functions use the standard CORS allowlist pattern per memory `mem://technical/edge-function-patterns`. Good.

**Recommendation:** add a lightweight health-check function or make `daily-digest` callable with `?dryRun=true` for monitoring without spamming Slack.

---

## 4. Code hygiene

| file | LOC | status |
|---|---:|---|
| `src/components/luna/ChatTab.tsx` | **1,184** | 🔴 **Too large.** Split out: `useChatPersistence`, `useChatStreaming`, `chatActionDetectors.ts` (the substring rule helpers), and the inline `LeadCaptureForm` subcomponent. Target: <500 LOC for the main component. |
| `src/components/LunaChatWidget.tsx` | 402 | 🟡 OK but trending. The two nudge `useEffect`s (~140 LOC) should be extracted to `useDwellNudge.ts` and `useInactivityNudge.ts` hooks. |
| `src/lib/conciergeStore.ts` | 407 | 🟡 The `buildLunaFirstMessage` and `buildConversationalSummary` belong in their own `lunaCopy.ts` file — they're presentation logic, not storage. |
| `src/lib/journeyTracker.ts` | 201 | 🟡 Borderline. Acceptable as-is but extract `getProactiveSuggestion` if it grows. |

No circular imports detected. No `any` casts beyond the documented `webkitAudioContext` shim.

---

## 5. Frontend / UX consistency check

Spot-checked against memory rules:

- ✅ "No Continue" copy rule — buttons use intent-driven labels.
- ✅ Site-wide phone `(520) 327-6753` consistent across `ChatTab`, `BookingDecisionCard`, `FooterSection`.
- ✅ Hours match memory (`mem://brand/business-hours`): Tue/Thu 9–7, Wed/Fri 9–5, Sat 9–4.
- ✅ No voice/ElevenLabs UI in current bundle (Luna is chat-only). Note: `ELEVENLABS_API_KEY` secret is still set but unused — safe to delete from Supabase secrets.
- ✅ Real photography only — no AI placeholders found in section components.
- ✅ Luna neutrality policy — `lunaBrain.ts` returns `null` for multi-provider artist recommendations.

---

## 6. Console warnings

Only one warning observed: `Unknown message type: RESET_BLANK_CHECK` from `cdn.gpteng.co/lovable.js`. **External (Lovable platform), not your code.** Ignore.

---

## 7. Recommended action list (ranked)

1. 🟡 **Tighten `site-assets` storage policy** — restrict `LIST` on `storage.objects` to service role; keep `SELECT` on individual files public.
2. 🟡 **Refactor `ChatTab.tsx`** — split into hook + helpers; smallest LOC change with biggest maintainability win.
3. 🟢 **Delete unused `sessions` table** + `ELEVENLABS_API_KEY` secret to reduce surface area.
4. 🟢 **Document the artists_public view** with a SQL comment explaining `direct_phone` is intentionally public.
5. 🟢 **Extract Luna nudge hooks** from `LunaChatWidget.tsx` (cosmetic, not blocking).
6. 🟢 **Add `?dryRun=true` to `daily-digest`** for safer monitoring.
7. 🟢 **Verify `hush-daily-digest` cron fires tomorrow at 8 AM AZ** — first scheduled run.

None of these are urgent. Items 1 and 2 are the only ones I'd pick up this week.
